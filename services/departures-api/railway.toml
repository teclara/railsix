[build]
builder = "RAILPACK"
watchPatterns = ["services/departures-api/**", "services/shared/**"]
checkSuites = "required"

[deploy]
startCommand = "./out"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
