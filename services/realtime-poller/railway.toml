[build]
builder = "RAILPACK"
buildCommand = "cd realtime-poller && go build -o /app/out ."
watchPatterns = ["services/realtime-poller/**", "services/shared/**"]
checkSuites = "required"

[deploy]
startCommand = "/app/out"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
