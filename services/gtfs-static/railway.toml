[build]
builder = "RAILPACK"
watchPatterns = ["services/gtfs-static/**", "services/shared/**"]
checkSuites = "required"

[deploy]
startCommand = "./out"
healthcheckPath = "/ready"
healthcheckTimeout = 420
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
